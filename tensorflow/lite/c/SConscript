from building import *

cwd     = GetCurrentDir()
src     = Glob('*.c') + Glob('*.cc')
CPPPATH = [cwd, str(Dir('#'))]

group = DefineGroup('lite', src, depend = ['BSP_USING_TFLITE'], CPPPATH = CPPPATH)

Return('group')
